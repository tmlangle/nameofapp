<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_tehrel %>
    <%= form.text_field :first_tehrel %>
  </div>

  <div class="field">
    <%= form.label :last_langley %>
    <%= form.text_field :last_langley %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
